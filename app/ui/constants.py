
TITLE: str = "Storage of Coords  [portals]  Library of Coords"
T_LINE: str = "—" * len(TITLE) # Title line = —————...
D_LINE: str = "-" * len(TITLE) # Default line = -----...
DELAY_XS: float = 0.015 # Mostly for flush/typing speed
DELAY_S: float = 0.1 # Mostly for short delay like in for loop
DELAY_M: float = 0.25 # Mostly for transition into other menu
